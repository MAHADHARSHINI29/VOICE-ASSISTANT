# Voice Assistant  
A cross-platform voice assistant application built with Flutter.

## 🧠 Project Overview  
This project implements a voice-assistant application that supports both Android and iOS (and possibly web/desktop if enabled). It allows users to speak commands, converts speech to text, interprets the command, and responds via speech or action.


## ✅ Features  
- Speech-to-text: Capture voice input from the microphone.  
- Command recognition: Interpret spoken input and map to predefined commands.  
- Text-to-speech: Speak responses back to the user.  
- Cross-platform: Works on Android and iOS (and optionally web/desktop).  
- Easily extensible: Add new voice commands or actions.

## 🚀 Getting Started  
### Prerequisites  
- Flutter SDK (version …) installed.  
- Xcode & iOS simulator (for iOS testing).  
- Android Studio / Android SDK (for Android testing).  
- A real device or simulator/emulator with microphone access.

### Installation & Setup  
1. Clone this repository:  
   ```bash
   git clone https://github.com/MAHADHARSHINI29/VOICE-ASSISTANT.git  
   cd VOICE-ASSISTANT
2. Install the dependencies
   flutter pub get
3. Run the app 
   flutter run  

